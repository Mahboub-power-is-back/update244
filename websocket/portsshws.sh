#!/bin/bash
echo "SSH WebSocket TLS is available on public port 443 at /ssh-ws/."
echo "SSH WebSocket is also available on public port 80 at /ssh-ws/."
echo "Ports 80/443 are shared by nginx and cannot be changed independently."
